// HEADER/INCLUDE GUARD //
#ifndef __CHAR_UTILS_HPP
#define __CHAR_UTILS_HPP

class CharUtils
{
public:
    static bool isAlpha(char c);

    static bool isDigit(char c);

    static bool isWhitespace(char c);

    static bool isAlphaNumeric(char c);
};

#endif